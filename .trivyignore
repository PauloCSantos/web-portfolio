# CloudFront WAF is intentionally not required for this static portfolio.
# Adding AWS WAF would add recurring cost and operational overhead that is not
# proportional to the current public static-site risk profile.
AWS-0011

# The site bucket uses SSE-S3 (AES256). A customer managed KMS key is not
# required for this non-sensitive static build artifact bucket at this stage.
AWS-0132
